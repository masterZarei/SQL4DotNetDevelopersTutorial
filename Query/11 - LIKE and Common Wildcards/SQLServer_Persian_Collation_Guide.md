
# ⚙️ مشکل Collation در جستجوی فارسی در SQL Server

اگر Collation پیش‌فرض دیتابیس را موقع نصب `Persian_100` انتخاب نکرده باشین، احتمالاً موقع جست‌وجوی کاراکترهای UTF-8 (از جمله کاراکترهای فارسی) به مشکل می‌خورین.

برای رفع این مشکل دو راه دارین:

---

## ✅ راه اول: تغییر Collation دیتابیس به فارسی

```sql
ALTER DATABASE نام_دیتابیس_شما
COLLATE Persian_100_CI_AS;
```

این روش، Collation پیش‌فرض کل دیتابیس رو به حالت فارسی تغییر می‌ده و از این به بعد کوئری‌ها بدون مشکل اجرا می‌شن.

---

## ✅ راه دوم: استفاده از پیشوند `N` برای رشته‌های Unicode

```sql
SELECT * FROM Customers
WHERE CustomerName LIKE N'%م';
```

وقتی از `N` قبل از رشته استفاده می‌کنید، SQL می‌فهمه که رشته‌ی شما Unicode هست (مناسب برای فارسی) و عملیات LIKE به درستی انجام می‌شه.

---

> این نکات ساده اما حیاتی می‌تونه کلی از دردسرهای جست‌وجوی فارسی در SQL Server رو حل کنه 😉
