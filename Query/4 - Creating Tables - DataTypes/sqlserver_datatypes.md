# 📊 پرکاربردترین دیتاتایپ‌های SQL Server برای توسعه‌دهندگان .NET

این جدول پرکاربردترین دیتاتایپ‌های **SQL Server** را همراه با توضیح کوتاه و معادل تقریبی آن‌ها در **C#** نشان می‌دهد.  
این مرجع برای برنامه‌نویسان ASP.NET Core بسیار کاربردی است.

| دیتاتایپ در SQL Server | توضیح کوتاه | سایز پیش‌فرض | معادل در #C |
|------------------------|-------------|--------------|-------------|
| `int`                 | عدد صحیح (±2,147,483,647) | 4 بایت | `int` |
| `bigint`              | عدد صحیح خیلی بزرگ | 8 بایت | `long` |
| `smallint`            | عدد صحیح کوچک (±32,767) | 2 بایت | `short` |
| `tinyint`             | عدد صحیح 0 تا 255 | 1 بایت | `byte` |
| `bit`                 | مقدار بولین (0 یا 1) | 1 بایت | `bool` |
| `decimal(p,s)` / `numeric(p,s)` | عدد دقیق با تعداد رقم اعشار مشخص | متغیر (5-17 بایت) | `decimal` |
| `float`               | عدد اعشاری تقریبی (دقیق نیست) | 8 بایت | `double` |
| `real`                | عدد اعشاری تقریبی کوچک‌تر | 4 بایت | `float` |
| `money`               | عدد اعشاری مخصوص مقادیر مالی | 8 بایت | `decimal` |
| `smallmoney`          | نسخه کوچک‌تر `money` | 4 بایت | `decimal` |
| `char(n)`             | رشته ثابت طول `n` | n بایت | `string` |
| `varchar(n)`          | رشته متنی متغیر طول | حداکثر 8,000 | `string` |
| `varchar(max)`        | رشته متنی طولانی | حداکثر 2GB | `string` |
| `nchar(n)`            | رشته Unicode ثابت طول | 2n بایت | `string` |
| `nvarchar(n)`         | رشته Unicode متغیر طول | حداکثر 4,000 | `string` |
| `nvarchar(max)`       | رشته Unicode خیلی بزرگ | حداکثر 2GB | `string` |
| `text` (قدیمی)        | متن طولانی (Deprecated) | حداکثر 2GB | `string` |
| `date`                | فقط تاریخ (YYYY-MM-DD) | 3 بایت | `DateTime` |
| `datetime`            | تاریخ و زمان (از سال 1753 تا 9999) | 8 بایت | `DateTime` |
| `datetime2`           | تاریخ و زمان دقیق‌تر | 6-8 بایت | `DateTime` |
| `smalldatetime`       | تاریخ و زمان محدود (1900 تا 2079) | 4 بایت | `DateTime` |
| `time`                | فقط زمان | 3-5 بایت | `TimeSpan` |
| `datetimeoffset`      | تاریخ و زمان با منطقه زمانی | 10 بایت | `DateTimeOffset` |
| `uniqueidentifier`    | شناسه یکتا (GUID) | 16 بایت | `Guid` |
| `binary(n)`           | داده باینری ثابت طول | n بایت | `byte[]` |
| `varbinary(n)`        | داده باینری متغیر طول | حداکثر 8,000 | `byte[]` |
| `varbinary(max)`      | داده باینری طولانی (مثل فایل‌ها) | حداکثر 2GB | `byte[]` |

---

## 💡 نکات آموزشی
- همیشه بین **دقت (Precision)** و **کارایی (Performance)** تعادل برقرار کنید.  
- برای پشتیبانی از زبان فارسی و Unicode از `nvarchar` استفاده کنید.  
- دیتاتایپ‌های قدیمی مثل `text`, `ntext`, `image` دیگر استفاده نمی‌شوند (Deprecated).  

