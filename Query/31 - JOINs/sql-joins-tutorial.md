# آموزش کامل JOIN در SQL Server

در این آموزش، می‌خواهیم به زبان ساده و با مثال، انواع **JOIN** در SQL Server را یاد بگیریم.
فرض می‌کنیم شما می‌دانید "رابطه" چیست، اما هنوز با انواع JOIN و کاربرد آن‌ها آشنا نیستید.

---

## 1. JOIN چیست؟
JOIN به ما اجازه می‌دهد داده‌ها را از چند جدول مرتبط **در یک نتیجه** ترکیب کنیم.
مثلاً اگر یک جدول `Customers` و یک جدول `Orders` داشته باشیم، می‌توانیم سفارش‌های هر مشتری را در یک کوئری نشان دهیم.

---

## 2. انواع JOIN

### 2.1 INNER JOIN
فقط رکوردهایی را برمی‌گرداند که **در هر دو جدول تطابق دارند**.

#### مثال:
```sql
SELECT Customers.CustomerName, Orders.OrderID
FROM Customers
INNER JOIN Orders
    ON Customers.CustomerID = Orders.CustomerID;
```
🧠 **توضیح:** فقط مشتریانی نمایش داده می‌شوند که سفارش ثبت کرده‌اند.

---

### 2.2 LEFT JOIN (یا LEFT OUTER JOIN)
تمام رکوردهای جدول سمت چپ و رکوردهای مطابق از جدول سمت راست را برمی‌گرداند.
اگر تطابقی نباشد، مقدار `NULL` نشان داده می‌شود.

#### مثال:
```sql
SELECT Customers.CustomerName, Orders.OrderID
FROM Customers
LEFT JOIN Orders
    ON Customers.CustomerID = Orders.CustomerID;
```
🧠 **توضیح:** همه مشتریان نمایش داده می‌شوند، حتی اگر سفارش نداشته باشند.

---

### 2.3 RIGHT JOIN (یا RIGHT OUTER JOIN)
برعکس LEFT JOIN عمل می‌کند.
همه رکوردهای جدول سمت راست و رکوردهای مطابق از جدول سمت چپ را برمی‌گرداند.

#### مثال:
```sql
SELECT Customers.CustomerName, Orders.OrderID
FROM Customers
RIGHT JOIN Orders
    ON Customers.CustomerID = Orders.CustomerID;
```
🧠 **توضیح:** همه سفارش‌ها نمایش داده می‌شوند، حتی اگر مشتری آن در جدول مشتریان نباشد.

---

### 2.4 FULL OUTER JOIN
همه رکوردهای هر دو جدول را برمی‌گرداند. اگر تطابقی نباشد، مقادیر NULL پر می‌شود.

#### مثال:
```sql
SELECT Customers.CustomerName, Orders.OrderID
FROM Customers
FULL OUTER JOIN Orders
    ON Customers.CustomerID = Orders.CustomerID;
```
🧠 **توضیح:** همه مشتریان و همه سفارش‌ها نمایش داده می‌شوند، حتی اگر هیچ ارتباطی بینشان نباشد.

---

### 2.5 CROSS JOIN
تمام ترکیب‌های ممکن بین رکوردهای دو جدول را برمی‌گرداند.
**هشدار:** تعداد رکوردها ممکن است خیلی زیاد شود.

#### مثال:
```sql
SELECT Customers.CustomerName, Orders.OrderID
FROM Customers
CROSS JOIN Orders;
```
🧠 **توضیح:** هر مشتری با هر سفارش ترکیب می‌شود.

---

## 3. نکات پایانی
- همیشه در JOIN‌ها شرط ON را دقیق بنویسید تا داده‌های اضافی نگیرید.
- برای مشاهده تمام رکوردهای هر دو جدول، FULL OUTER JOIN بهترین انتخاب است.
- برای ترکیب همه داده‌ها بدون شرط، از CROSS JOIN استفاده کنید.

---

**🎯 نتیجه:** با استفاده از JOIN می‌توانیم داده‌های چند جدول را به شکلی منظم و هدفمند ترکیب کنیم و گزارش‌های کاربردی بسازیم.
