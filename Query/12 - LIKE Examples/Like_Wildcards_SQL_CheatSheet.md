
# 🎯 آموزش LIKE و Wildcard ها در SQL

---

## ✅ شروع با حرف خاص

انتخاب تمام مشتریانی که نام آن‌ها با حرف a شروع می‌شود:
```sql
SELECT * FROM Customers
WHERE CustomerName LIKE 'a%';
```

الگو: `'a%'` → هر چیزی که با a شروع شود و ادامه داشته باشد.

---

## ✅ ترکیب چند Wildcard


 تمام مشتریانی که شهر آن‌ها با 'L' شروع شود، بعد یک حرف دلخواه بیاید،
سپس 'nd' و بعد از آن دو حرف دیگر قرار گیرد:
```sql
SELECT * FROM Customers
WHERE City LIKE 'L_nd__';
```

الگو: `'L_nd__'`  
توضیح:  
- `L` = ثابت  
- `_` = یک کاراکتر  
- `nd` = ثابت  
- `__` = دو کاراکتر دلخواه

---

## ✅ شامل بودن یک حرف خاص در هر موقعیتی

تمام مشتریانی که در نام شهر آن‌ها حرف 'L' وجود دارد:
```sql
SELECT * FROM Customers
WHERE City LIKE '%L%';
```

الگو: `'%L%'` → شامل حرف L در هر موقعیت.

---

## ✅ شروع با دو حرف خاص

مشتریانی که نام آن‌ها با 'La' شروع می‌شود:
```sql
SELECT * FROM Customers
WHERE CustomerName LIKE 'La%';
```

الگو: `'La%'` → شروع با "La"

---

## ✅ پایان با یک حرف خاص

مشتریانی که نام آن‌ها با حرف 'a' تمام می‌شود:
```sql
SELECT * FROM Customers
WHERE CustomerName LIKE '%a';
```

الگو: `'%a'` → پایان با a

---

## ✅ شروع و پایان خاص هم‌زمان

مشتریانی که با b شروع و با s پایان می‌یابند:
```sql
SELECT * FROM Customers
WHERE CustomerName LIKE 'b%s';
```

الگو: `'b%s'` → شروع با b و پایان با s

---

## ✅ حداقل ۳ کاراکتر پس از حرف اول

مشتریانی که نام آن‌ها با a شروع می‌شود و حداقل ۳ کاراکتر دارد:
```sql
SELECT * FROM Customers
WHERE CustomerName LIKE 'a__%';
```

الگو: `'a__%'` → a + دو کاراکتر اجباری + ادامه اختیاری

---

## ✅ وجود حرف خاص در جایگاه دوم

مشتریانی که دومین حرف از نام آن‌ها r است:
```sql
SELECT * FROM Customers
WHERE CustomerName LIKE '_r%';
```

الگو: `'_r%'` → هر حرفی + r + ادامه دلخواه

---

## ✅ جستجوی مقدار دقیق بدون wildcard

مشتریانی که کشور آن‌ها دقیقاً Spain است:
```sql
SELECT * FROM Customers
WHERE Country LIKE 'Spain';
```

الگو: `'Spain'` → هیچ wildcard استفاده نشده، یعنی جستجوی دقیق

---

### 📌 نکات پایانی

| نماد | توضیح |
|------|--------|
| `%`  | جایگزین صفر یا چند کاراکتر دلخواه |
| `_`  | دقیقاً یک کاراکتر دلخواه |

